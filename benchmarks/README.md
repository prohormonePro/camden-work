# Continuity evaluation assets

These source-tree assets are separate from the immutable v0.1.0 release. See docs/operator-externalization.md and docs/continuity-gauntlet.md. The Windows maintainer run includes all four started episodes, raw JSON before/after snapshots and local frozen/delivered reports. Databases are reproducible by running the command, not committed binaries. Runtime source identity is bound by SHA-256 in each plan. Harness assistance is explicitly counted. No competitor or remote-provider trial is included.
